/**
 * A collection of possible error and warning messages.
 */
/* istanbul ignore next */
export const ErrorMessage = {
  NotSerializable() {
    return `memoizer key derivation failed: failed to serialize id component`;
  }
};
